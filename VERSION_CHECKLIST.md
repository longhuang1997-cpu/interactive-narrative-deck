# 版本号更新清单

每次修改后，手动改3处（CTRL+F搜索 `version:`）：

## ✅ 必改的3个位置

1. **SKILL.md** 第3行
   ```yaml
   version: 2.0.x  ← 改这里
   ```

2. **marketplace.json** 第11行
   ```json
   "version": "2.0.x",  ← 改这里
   ```

3. **marketplace.json** 第27行
   ```json
   "version": "2.0.x"  ← 改这里（注意：这行末尾无逗号）
   ```

## 版本号规则

- 文档更新/Bug修复 → 递增最后一位（2.0.0 → 2.0.1）
- 新功能/新Block → 递增中间一位（2.0.1 → 2.1.0）
- 架构重构 → 递增第一位（2.1.0 → 3.0.0）

## 当前版本：2.0.1
