# Interactive Narrative Deck Skill 安装指引

> **适用于：Claude Code 用户（使用 Codex 客户端）**

## 📦 这个包是什么

这是一个 Claude Code Skill，用于生成"结构化交互叙事演示"——比 PPT 更活、比游戏更专业的职场汇报工具。

**核心能力：**
- Block 积木式组件（标题/指标/对比/图表/时间线/引用）
- 渐进揭示 + 数字滚动 + 图表生长动画
- 单文件 HTML，翻页笔可用，离线可用

## ⚡ 3步安装（<2分钟）

### 方式1：命令行安装（推荐，最快）

```bash
# 1. 打开终端/PowerShell，进入解压后的目录
cd path/to/interactive-narrative-deck-skill-package

# 2. 复制到 Claude Code skills 目录
# Windows 用户：
xcopy /E /I /Y . "%USERPROFILE%\.claude\skills\interactive-narrative-deck"

# macOS/Linux 用户：
cp -r . ~/.claude/skills/interactive-narrative-deck

# 3. 验证安装
ls ~/.claude/skills/interactive-narrative-deck  # 应该看到 SKILL.md
```

### 方式2：手动安装

1. 找到你的 Claude Code skills 目录：
   - **Windows**: `C:\Users\你的用户名\.claude\skills\`
   - **macOS/Linux**: `~/.claude/skills/`

2. 在 skills 目录里创建文件夹：`interactive-narrative-deck`

3. 把本安装包里的**所有文件**复制进去：
   ```
   interactive-narrative-deck/
   ├── SKILL.md              ← 必须
   ├── README.md
   ├── LICENSE
   ├── .claude-plugin/
   ├── engine/
   └── templates/
   ```

4. 确认 `SKILL.md` 在正确位置：
   ```
   ~/.claude/skills/interactive-narrative-deck/SKILL.md
   ```

## ✅ 验证安装

重启 Claude Code（Codex），然后在对话中说：

```
帮我做一个战略汇报演示，包含：封面、核心指标、趋势图、问题分析、解决方案对比
```

Claude 应该会：
1. 识别出这是"汇报演示"场景
2. 自动调用 `interactive-narrative-deck` skill
3. 生成 `index.html` + `deck.js` 文件

## 🎯 使用示例

安装后，你可以这样调用：

### 示例1：快速生成
```
用 interactive-narrative-deck 做一个产品发布会演示，
包含：产品亮点、功能对比、上市计划
```

### 示例2：修改现有演示
```
把 deck.js 里的第2页改成柱状图，数据用我上面提供的Q1-Q4销售额
```

### 示例3：定制配色
```
把主题色改成：主色#1e40af（深蓝）、强调色#f59e0b（琥珀金）
```

## 📖 快速参考

### Block 组件速查表

| Block类型 | 用途 | 示例 |
|----------|------|------|
| `hero` | 封面/章节标题 | `{ type:"hero", title:"标题", sub:"副标题" }` |
| `metric` | 数据指标（数字滚动） | `{ type:"metric", items:[{value:"1280",unit:"万",label:"营收"}]}` |
| `bullets` | 要点列表 | `{ type:"bullets", items:["要点1","要点2"], stagger:true }` |
| `compare` | 左右对比 | `{ type:"compare", left:{...}, right:{...} }` |
| `timeline` | 时间线 | `{ type:"timeline", items:[{time:"Q1",text:"里程碑"}]}` |
| `chart` | 图表 | `{ type:"chart", chart:"line", data:{...} }` |
| `quote` | 金句引用 | `{ type:"quote", text:"引用", by:"出处" }` |

### 键盘控制

- **左右键 / PageUp PageDown** - 翻页
- **空格** - 渐进揭示（逐个显示 block）
- **O键** - 总览模式（显示所有页缩略图）
- **F11** - 全屏
- **Home / End** - 跳到首页/末页

## 🔧 常见问题

**Q: 安装后 Claude 不识别这个 skill？**  
A: 检查 3 点：
1. `SKILL.md` 文件在 `~/.claude/skills/interactive-narrative-deck/SKILL.md` 吗？
2. 重启了 Claude Code 吗？
3. 提示词是否清楚（包含"演示""汇报""presentation"等关键词）？

**Q: 生成的 HTML 打开是空白？**  
A: 检查：
1. `index.html` 和 `deck.js` 在同一目录吗？
2. `engine/` 目录在上级目录吗？（相对路径 `../engine/`）
3. 浏览器控制台有报错吗？（F12 查看）

**Q: CDN 加载失败？**  
A: 引擎内置降级机制，GSAP/Chart.js 失败会自动切到 CSS 动画 + 文字。离线使用需下载库到本地。

**Q: 能用于商业项目吗？**  
A: 可以，MIT 协议，自由使用/修改/分发。

## 📞 支持与反馈

- **GitHub**: https://github.com/longhuang1997-cpu/interactive-narrative-deck
- **Issues**: 报告 bug 或功能建议
- **Skill市场**: [比赛页面链接]

---

**版本**: 2.0.0 | **协议**: MIT